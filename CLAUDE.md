# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`xvulture9270/dentipay`) is currently empty — no source files, no commits, no history. The remote `origin` points to `xvulture9270/dentipay` but contains no branches yet.

Until code is added, there is no build system, test suite, lint configuration, or architecture to document. When the first code lands, replace this section with:

- **Commands**: build / test / lint / run-single-test invocations specific to the chosen stack.
- **Architecture**: the module boundaries and data flow that span multiple files (the things a reader can't infer from a single file).
- **Conventions**: any non-obvious patterns the codebase enforces.

## Working Branch

All development for AI-assisted changes in this session must go to the branch `claude/add-claude-documentation-OSNRW`. Do not push to other branches without explicit permission.
